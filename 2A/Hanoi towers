def move(n, start, finish):
    if n == 1:
        print(1, start, finish)
    else:
        temp = 6 - start - finish
        move(n - 1, start, temp)
        print(n, start, finish)
        move(n - 1, temp, finish)

move(int(input()), 1, 3)
